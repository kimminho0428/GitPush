import React from 'react';
import {useToasts} from "react-toast-notifications";

const COSGN00_COSGN0AValidation = ({  USERID, USERIDRef, PASSWD, PASSWDRef, }) => {
    // const {addToast} = useToasts();

    const validate = () => {
        return true;
    }

    return validate();

};
export default COSGN00_COSGN0AValidation;

