import React from 'react';
import {useToasts} from "react-toast-notifications";

const COCRDSL_CCRDSLAValidation = ({  ACCTSID, ACCTSIDRef, CARDSID, CARDSIDRef, }) => {
    // const {addToast} = useToasts();

    const validate = () => {
        return true;
    }

    return validate();

};
export default COCRDSL_CCRDSLAValidation;

