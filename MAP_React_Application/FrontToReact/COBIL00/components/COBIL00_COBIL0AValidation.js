import React from 'react';
import {useToasts} from "react-toast-notifications";

const COBIL00_COBIL0AValidation = ({  ACTIDIN, ACTIDINRef, CONFIRM, CONFIRMRef, }) => {
    // const {addToast} = useToasts();

    const validate = () => {
        return true;
    }

    return validate();

};
export default COBIL00_COBIL0AValidation;

