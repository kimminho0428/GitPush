package aws.carddemo.file;

import aws.carddemo.dao.*;
import aws.carddemo.dto.*;
import aws.carddemo.mapper.*;
import aws.carddemo.variable.group.Cbact04cFdTranCatBalRecord;
import com.tmax.openframe.runtime.context.OpenFrameContext;
import com.tmax.openframe.runtime.file.FileStatus;
import com.tmax.openframe.runtime.file.KeyOperationType;
import com.tmax.openframe.runtime.file.RecordCommitter;
import java.math.*;
import java.util.*;
import lombok.*;
import org.apache.ibatis.executor.BatchResult;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Repository;

/** 
 * <p>This source code was generated by T-Up OpenFrame COBOL to Java migrator</p>
 * <p>  Generation date : 2024/05/02 05:55:55 UTC</p>
 * <hr>
 * <p>Class for processing file I/O operations</p>
 * <ul>
 * <li>target dataset: AwsM2CarddemoTcatbalfVsamKsds</li>
 * <li>file operation class: {@link Cbact04cFileOpTcatbalFile}</li>
 * <li>DAO class: {@link AwsM2CarddemoTcatbalfVsamKsdsDao}</li>
 * <li>DTO class: {@link AwsM2CarddemoTcatbalfVsamKsdsDto}</li>
 * </ul>
 */
@Repository
public class Cbact04cFdTcatbalFile extends Cbact04cFileOpTcatbalFile {
    @Autowired
    private AwsM2CarddemoTcatbalfVsamKsdsDao dao;
    @Autowired
    private RecordCommitter<AwsM2CarddemoTcatbalfVsamKsdsDto> committer;

    public void open(OpenFrameContext context) {
        setIterator(context, dao.readAllAsc().iterator());
        setFileStatus(context, FileStatus.NORMAL);
        context.initFileWriteBuffer("Cbact04c", "TcatbalFile");
        committer.setThreshold(5000);
    }

    public void start(OpenFrameContext context,
            Cbact04cFdTranCatBalRecord.Cbact04cFdTranCatKey key,
            KeyOperationType type) {
        Cbact04cFdTranCatBalRecord startPoint = new Cbact04cFdTranCatBalRecord();
        startPoint.setFdTranCatKey(key);
        AwsM2CarddemoTcatbalfVsamKsdsDto dto = Cbact04cMapperInterface.Cbact04cFdTranCatBalRecordKeyToAwsM2CarddemoTcatbalfVsamKsdsDto.INSTANCE
                .toTarget(startPoint);
        switch (type) {
        case EQ:
        case GTEQ: {
            setIterator(context, dao.readGeDto(dto).iterator());
            break;
        }
        case GT: {
            setIterator(context, dao.readGtDto(dto).iterator());
            break;
        }
        default: {
            throw new RuntimeException(
                    "not supported operation for start method");
        }
        }
        if (!getIterator(context).hasNext()) {
            setFileStatus(context, FileStatus.INVALID_KEY_READ);
        } else {
            setFileStatus(context, FileStatus.NORMAL);
        }
    }

    public Cbact04cFdTranCatBalRecord readNextRecord(OpenFrameContext context) {
        Iterator<AwsM2CarddemoTcatbalfVsamKsdsDto> iterator = getIterator(context);
        if (iterator.hasNext()) {
            AwsM2CarddemoTcatbalfVsamKsdsDto nextRecord = iterator.next();
            if (nextRecord == null) {
                setFileStatus(context, FileStatus.AT_END_CONDITION_SEQ_READ);
                return null;
            }
            setFileStatus(context, FileStatus.NORMAL);
            return Cbact04cMapperInterface.Cbact04cFdTranCatBalRecordToAwsM2CarddemoTcatbalfVsamKsdsDto.INSTANCE
                    .toSource(nextRecord);
        } else {
            setFileStatus(context, FileStatus.AT_END_CONDITION_SEQ_READ);
            return null;
        }
    }

    public Cbact04cFdTranCatBalRecord readKey(OpenFrameContext context,
            Cbact04cFdTranCatBalRecord.Cbact04cFdTranCatKey key) {
        commit(context);
        Cbact04cFdTranCatBalRecord readKeyPoint = new Cbact04cFdTranCatBalRecord();
        readKeyPoint.setFdTranCatKey(key);
        AwsM2CarddemoTcatbalfVsamKsdsDto dto = Cbact04cMapperInterface.Cbact04cFdTranCatBalRecordKeyToAwsM2CarddemoTcatbalfVsamKsdsDto.INSTANCE
                .toTarget(readKeyPoint);
        Iterator<AwsM2CarddemoTcatbalfVsamKsdsDto> iterator = dao
                .readEqDto(dto).iterator();
        setIterator(context, iterator);
        if (iterator.hasNext()) {
            setFileStatus(context, FileStatus.NORMAL);
            return Cbact04cMapperInterface.Cbact04cFdTranCatBalRecordToAwsM2CarddemoTcatbalfVsamKsdsDto.INSTANCE
                    .toSource(iterator.next());
        } else {
            setFileStatus(context, FileStatus.INVALID_KEY_READ);
            return null;
        }
    }

    public void write(OpenFrameContext context, Cbact04cFdTranCatBalRecord data) {
        List<AwsM2CarddemoTcatbalfVsamKsdsDto> writeBuffer = getWriteBuffer(context);
        writeBuffer
                .add(Cbact04cMapperInterface.Cbact04cFdTranCatBalRecordToAwsM2CarddemoTcatbalfVsamKsdsDto.INSTANCE
                        .toTarget(data));
        if (writeBuffer.size() >= committer.getThreshold()) {
            committer.commit(dao::writeDto, writeBuffer);
            writeBuffer.clear();
        }
        setFileStatus(context, FileStatus.NORMAL);
    }

    public void rewrite(OpenFrameContext context,
            Cbact04cFdTranCatBalRecord data,
            Cbact04cFdTranCatBalRecord.Cbact04cFdTranCatKey key) {
        commit(context);
        data.setFdTranCatKey(key);
        AwsM2CarddemoTcatbalfVsamKsdsDto updateDto = Cbact04cMapperInterface.Cbact04cFdTranCatBalRecordToAwsM2CarddemoTcatbalfVsamKsdsDto.INSTANCE
                .toTarget(data);
        dao.flush();
        dao.rewriteDto(updateDto);
        List<BatchResult> resultList = dao.flush();
        if (resultList.get(0).getUpdateCounts()[0] > 0) {
            setFileStatus(context, FileStatus.NORMAL);
        } else {
            setFileStatus(context, FileStatus.INVALID_KEY_CREATE);
        }
    }

    public void delete(OpenFrameContext context,
            Cbact04cFdTranCatBalRecord.Cbact04cFdTranCatKey key) {
        if (getIterator(context) == null) {
            return;
        }
        commit(context);
        Cbact04cFdTranCatBalRecord deletePoint = new Cbact04cFdTranCatBalRecord();
        deletePoint.setFdTranCatKey(key);
        AwsM2CarddemoTcatbalfVsamKsdsDto dto = Cbact04cMapperInterface.Cbact04cFdTranCatBalRecordKeyToAwsM2CarddemoTcatbalfVsamKsdsDto.INSTANCE
                .toTarget(deletePoint);
        dao.flush();
        dao.deleteDto(dto);
        List<BatchResult> resultList = dao.flush();
        if (resultList.get(0).getUpdateCounts()[0] > 0) {
            setFileStatus(context, FileStatus.NORMAL);
        } else {
            setFileStatus(context, FileStatus.INVALID_KEY_READ);
        }
    }

    public void close(OpenFrameContext context) {
        commit(context);
        setIterator(context, null);
        setFileStatus(context, FileStatus.NORMAL);
    }

    public void commit(OpenFrameContext context) {
        List<AwsM2CarddemoTcatbalfVsamKsdsDto> writeBuffer = getWriteBuffer(context);
        if (writeBuffer != null && writeBuffer.size() > 0) {
            committer.commit(dao::writeDto, writeBuffer);
            writeBuffer.clear();
        }
    }

    private void setIterator(OpenFrameContext context,
            Iterator<AwsM2CarddemoTcatbalfVsamKsdsDto> iterator) {
        context.setFileIterator("Cbact04c", "TcatbalFile", iterator);
    }

    private Iterator<AwsM2CarddemoTcatbalfVsamKsdsDto> getIterator(
            OpenFrameContext context) {
        return context.getFileIterator("Cbact04c", "TcatbalFile");
    }

    private List<AwsM2CarddemoTcatbalfVsamKsdsDto> getWriteBuffer(
            OpenFrameContext context) {
        return context.getFileWriteBuffer("Cbact04c", "TcatbalFile");
    }
}
