import React from 'react';
import {useToasts} from "react-toast-notifications";

const COUSR02_COUSR2AValidation = ({  USRIDIN, USRIDINRef, FNAME, FNAMERef, LNAME, LNAMERef, PASSWD, PASSWDRef, USRTYPE, USRTYPERef, }) => {
    // const {addToast} = useToasts();

    const validate = () => {
        return true;
    }

    return validate();

};
export default COUSR02_COUSR2AValidation;

